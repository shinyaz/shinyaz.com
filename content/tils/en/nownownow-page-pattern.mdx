---
title: "nownownow.com's Now page is a useful complement to an About page"
description: "A Now page shows what you're currently focused on, unlike a static About page. Adding lastUpdated frontmatter with Intl.DateTimeFormat gives locale-aware date display for free."
date: "2026-03-14"
published: true
tags:
  - web-design
---

I knew about About pages and Uses pages, but I hadn't heard of the "Now page" concept from [nownownow.com](https://nownownow.com) until recently. The idea is simple: About tells visitors _who you are_, while Now tells them _what you're doing right now_. You update it periodically rather than per-event like blog posts.

For implementation, the key addition was a `lastUpdated` field in the Velite page schema:

```typescript
lastUpdated: s.isodate().optional()
```

Making it `optional` means existing pages (About, Uses, Colophon) are unaffected. Then in the route, `Intl.DateTimeFormat` handles locale-aware formatting without any library:

```tsx
{new Intl.DateTimeFormat(locale, {
  year: "numeric",
  month: "long",
  day: "numeric",
}).format(new Date(page.lastUpdated))}
```

This outputs "March 14, 2026" for English and "2026年3月14日" for Japanese automatically.
